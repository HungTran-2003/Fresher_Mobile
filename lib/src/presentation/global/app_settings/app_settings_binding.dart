import 'package:get/get.dart';
import 'app_settings_controller.dart';

class AppSettingsBinding extends Bindings {
  @override
  void dependencies() {
    // AppSettingsController is registered in GlobalBinding
  }
}
